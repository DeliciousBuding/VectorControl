## 为什么

`ROADMAP.md` 中“飞书抓取治理 v2”仍未关单，但当前仓库已经具备飞书通道的基本发送能力与测试消息链路。现在需要补齐的是发送侧治理：频控、失败隔离、状态缓存过期与结构化审计日志。

## 变更内容

1. 建立 `feishu-governance-v2-phase1` change 骨架。
2. 为 `FeishuSender` 增加发送侧频控与失败隔离。
3. 为治理状态增加过期清理与结构化日志。
4. 通过 sender smoke 覆盖节流与隔离场景。

## 影响

- 降低飞书发送异常时的放大效应。
- 为后续更细治理（如持久化审计）打下统一入口。
