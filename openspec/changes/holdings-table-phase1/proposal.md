## 为什么

`ROADMAP.md` 中“持仓表头自定义字段”仍是待办，但当前实现已经具备列显示/隐藏能力，只缺少“零份额持仓隐藏”的收口与正式回归。

需要把这条能力从“半完成”推进到“已实现、有测试、有状态同步”。

## 变更内容

1. 建立持仓表格能力的 OpenSpec change 骨架。
2. 为 `HoldingsTable` 增加零份额持仓隐藏能力。
3. 用最小回归测试覆盖 50+ 列表、编辑、零份额隐藏。

## 影响

- 让持仓页对空仓行的展示更干净。
- 让 `ROADMAP.md` 与当前实现保持一致。
