## 为什么

`ROADMAP.md` 中“基金专属独立页面”与“优化基金详情页信息密度”两条需求已经有实际实现，但缺少对应的 OpenSpec 入口与正式回归证明，导致任务仍停留在待办态。

当前需要把这两条需求从“代码已存在”推进到“有契约、有验证、有状态同步”的闭环。

## 变更内容

1. 建立基金详情独立页的 OpenSpec change 骨架。
2. 增加 `FundDetailPage` 的最小前端回归测试。
3. 同步更新 `ROADMAP.md` 与 `docs/最新进度.md`，将已实现项从待办转为已完成。

## 影响

- 让 `/fund/:fund_id` 独立路由与详情页左右布局具备正式回归证据。
- 让路线图状态与当前代码现实一致。
