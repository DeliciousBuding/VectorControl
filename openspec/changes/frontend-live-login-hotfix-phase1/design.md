## 设计目标

这是一个线上热修复闭环，目标只有一个：恢复登录后首页的可用性。

## 设计原则

1. **只修根因**：补齐缺失导入，不扩散到无关重构。
2. **先验证再部署**：本地回归通过后再发往 `prod`。
3. **保留证据**：使用 Playwright 记录登录前后状态，确保问题真正关闭。

## 非目标

- 本轮不继续做新的视觉设计改动。
- 本轮不调整收益面板 UI。
