# VectorControl Roadmap

更新时间：2026-02-06  
产品定位：`VectorControl = 管行为（Decision -> Action -> Review -> Discipline）`，不做预测、不做情绪化内容、不做社区。

## 0. 当前新增需求（已确认）

1. 引入用户登录与会话机制，支持注册/登录/退出。
2. 每个用户数据完全隔离：持仓、估值快照、执行记录、日报互不影响。
3. 前端主视图改为持仓中心，国内与港股板块置顶，美股与海外板块置后。
4. 增强可视化：总持仓波形图 + 单基金波形图，支持用户维度数据查看。
5. 前端设置中心：每个用户在前端配置显示偏好、筛选排序偏好、通知预留参数。
6. 通知预留：飞书 webhook 与邮件 SMTP 参数先存储，后续直接接入调度发送器。

## 0.1 前端工作台增强（当前迭代执行）

1. 设置中心（用户级）
- 显示设置：字体大小、默认筛选、默认排序、板块顺序、自动定位基金。
- 通知预留：飞书（启用开关、webhook、预判/复盘时间）、邮件（SMTP/发件人/收件人/TLS）。
- 所有设置按用户隔离存储，切换账户后自动载入对应设置。

2. 低摩擦交互
- 快速筛选：全部、仅上涨、仅下跌、仅异常。
- 快速排序：市值、今日收益、持有收益、涨跌幅、基金名称。
- 国内与港股默认置顶展示，美股与海外置后；支持设置中切换顺序。

3. 状态与可观测性
- 登录态与刷新态分离，错误提示统一收敛到状态栏。
- 估值异常基金可直接筛选，避免在大表格中手工查找。

## 0.2 前端易用性升级（本轮新增）

1. 自动刷新交互
- 设置中心新增自动刷新开关、刷新间隔（15-600 秒）、页面不可见时暂停刷新。
- 登录态工具区支持一键开启/关闭自动刷新，并即时持久化到用户设置。
- 自动刷新与手动刷新共用同一数据链路，前端防重复请求，避免并发拉取。

2. 纯中文展示与可读性
- 前端状态文案、按钮文案、数据来源文案保持中文，减少混合语言噪音。
- 数据来源统一映射：`eastmoney/tencent` -> `东方财富/腾讯`。
- 字号默认保持大号，可在设置中心切换中/大/特大。

3. 持仓中心强化
- 继续保持“国内与港股在上，美股与海外在下”的默认信息架构。
- 持仓表聚焦市值、今日涨跌、今日收益、持有收益、成本、数据状态。
- 图表区保留总持仓波形与单基金波形，作为日常 2-5 分钟复查入口。

## 0.3 前端功能性继续迭代（本轮执行）

1. 工作台效率增强
- 新增收藏基金机制（星标），支持“仅看收藏”模式并按用户设置持久化。
- 新增“按收藏优先”排序，适配日常重点基金盯盘场景。
- 新增键盘快捷键：`/` 聚焦搜索，`R` 触发刷新（输入框内不触发）。

2. 数据浏览与导出
- 工具区新增一键导出当前视图 CSV（含代码、名称、市值、涨跌幅、收益、状态、来源）。
- 工具区新增异常快速入口（显示异常数量并一键切换异常筛选）。
- 新增分组概览卡：国内与港股 / 美股与海外分别展示市值与今日收益。

3. 表格可读性与交互
- 新增表格密度选项（紧凑/标准/宽松），按用户设置持久化。
- 持仓行支持选中高亮，点击行即可切换单基金波形。
- 异常状态显示强化：原因 + 数据来源同屏展示。

## 0.4 改进建议评估（参考主流基金产品交互）

说明：本节用于持续筛选“高价值、低摩擦”的前端能力，优先落地能提升日常 2-5 分钟复盘效率的功能。

1. 走势周期切换 + 图表样式切换（已落地）
- 建议：支持 `当日/近一周/近一月/成立来`，并支持 `波形图/类K线` 两种视图。
- 参考：主流基金产品常见“分周期查看 + 图表形态切换”模式（如支付宝基金、天天基金）。
- 评估：用户价值高，研发成本中，风险低。
- 状态：本轮已实现，并可在设置中心配置默认周期与样式。

2. 筛选快线与批量导出（已落地）
- 建议：异常一键筛选、收藏优先、CSV 导出当前视图。
- 评估：用户价值高，研发成本低，风险低。
- 状态：本轮已实现。

3. 基金详情抽屉（下一步）
- 建议：单基金展开后展示“来源、asof、失败原因历史、最近执行动作”。
- 评估：用户价值中高，研发成本中，风险低。
- 状态：计划进入下一轮。

4. 持仓 CRUD（下一步）
- 建议：前端新增/编辑/归档持仓，后端保留审计字段，禁止直接改历史事实成本。
- 评估：用户价值高，研发成本中高，风险中。
- 状态：作为 P0.5/P1 前置能力。

5. 推送联动入口（预留增强）
- 建议：设置中心增加“飞书/邮件测试发送”按钮，先打通手动触发再接调度。
- 评估：用户价值中，研发成本中，风险低。
- 状态：已预留参数，待后端发送器接入。

## 1. 优化后的路线图原则

1. 先打透估值可靠性和执行闭环，再扩展自动化与多组合。
2. 每个阶段都可独立上线，不能依赖“下一阶段才补齐”。
3. 所有动作都要可追溯：`策略版本`、`触发原因`、`执行记录`、`事后结果`。
4. 估值系统优先级高于展示样式，风控优先级高于推送。

## 2. 参考实现提炼（来自本地参考项目）

- `FundVal-Live`：可借鉴 `T+1 确认交易流水`、`分时快照采集`、`定时任务调度`、`订阅提醒控制`，用于补齐执行与自动化闭环。
- `real-time-fund`：可借鉴 `估值缺失时降级展示`、`前端实时可视化与移动端适配`，用于提升估值异常场景可用性。
- 以上能力只提炼工程机制，不引入其“社区/AI结论/情绪化叙事”方向。

## 3. 当前基线（来自本项目实现）

### 已有能力

- 资产与持仓：`config/portfolio.yaml` 管理多基金持仓，含 `bucket/tags/成本/市值/起始日`。
- 估值引擎：主源东财 + 回退腾讯；单基金含 `status/reason/source/asof`。
- 聚合能力：桶级加权估值，支持部分基金失败仍可聚合。
- 策略建议：`policy.yaml` 阈值 + 固定动作/条件动作，返回原因说明。
- 执行记录：`/api/actions` 已支持动作写入与查询。
- 日报：`/api/report/daily` 已输出估值摘要与执行摘要。

### 主要缺口

- 缺少“今日结论层”（可执行/观察/不动）和动作风险分级。
- 执行链路缺少 T+1 生效确认、失败/回滚、冷却期等纪律约束。
- 复盘仍是摘要级，缺少“执行 vs 不执行”对照和纪律评分。
- 策略未版本化，缺少可回溯的策略-结果关联。
- 推送与自动化尚未进入工程化阶段。

## 4. 估值模块硬性要求（P0 Gate）

以下要求在 P0 必须全部满足并保持回归测试覆盖：

1. 单基金返回字段完整：`status`、`reason`、`source`、`asof`。
2. 主源失败必须可回退，且外部请求有超时控制。
3. 单基金失败不能拖垮桶级聚合与组合输出。
4. 桶级结果必须体现置信度/覆盖率信息。
5. 回归测试至少覆盖：主源成功、回退成功、部分失败可聚合。

## 5. 分期里程碑

## P0（当前迭代，优先级最高）估值可靠性 + 今日决策闭环

目标：把“能看”升级为“今天可以按纪律执行”。

交付项：

- `Decision v1`：新增 Today Verdict（`可执行/观察/不动`）与一句话结论。
- `Action v1`：动作增加 `risk_level(low/medium)`、`why`、`trigger_source`。
- `Execution Log v1`：执行记录增加 `executed_at`、`planned_amount`、`actual_amount`、`result(done/failed)`。
- `Constraint v1`：单日最大操作次数、同标的冷却期、禁止连续加仓。
- `Report v1`：日报增加“未执行原因”和“明日关注点”。
- 前端看板对齐：仅显示可执行动作、风险上升资产过滤、估值异常显式提示。

验收标准：

- 用户在一个页面可完成：刷新估值 -> 查看 Verdict -> 记录执行 -> 查看日报。
- 任意一只基金估值失败时，组合级决策仍可产出（含降级说明）。

## P1（下一阶段）执行生效 + 复盘纪律闭环

目标：把“记录动作”升级为“验证动作是否改善组合”。

交付项：

- `T+1/T+N Confirm`：引入确认日机制，动作先入账、净值公布后确认生效（参考 `FundVal-Live` 的交易确认链路）。
- `Transaction Ledger`：新增加仓/减仓流水表，支持 pending -> applied 状态迁移。
- `Review v1`：执行前后成本变化、风险变化、回撤影响评估。
- `No-Action Baseline`：生成“若今日不操作”的对照收益/回撤。
- `Discipline Log`：违规动作（超阈值、冷却期内操作）自动标记。
- `Risk View v1`：桶级暴露、市场暴露（A股/美股/QDII/汇率代理）、集中度指标。
- `Policy Versioning v1`：策略哈希、版本说明、生效时间、历史回放。

验收标准：

- 任一执行动作都能追溯到：触发策略版本 -> 执行记录 -> 确认生效 -> 复盘结果。
- 可以查询任意日期的“实际执行 vs 不执行”差异。

## P2（后续阶段）自动化运营 + 多组合扩展

目标：降低手工维护成本，提升长期复盘价值。

交付项：

- `Scheduler v1`：交易时段分时快照采集、每日归档、历史清理。
- `Push v1`：14:50 预判 + 15:10 复盘，且“仅有变化才推送”。
- `Push Control`：静默日、仅工作日、节假日前抑制。
- `Multi-Portfolio`：主组合/卫星组合隔离，独立 policy。
- `Annual Report`：年度纪律评分、最大回撤阶段复盘、过度操作分析。

验收标准：

- 开启自动化后，用户可在不盯盘情况下收到纪律化摘要，不引入噪音提醒。
- 新增组合不影响原组合策略与历史数据。

## 6. 明确不做（写入长期边界）

- 不做涨跌预测与择时预测结论。
- 不做情绪指数、热榜导向功能。
- 不做社区、跟单、排行榜。
- 不输出“AI 炒股结论”类内容。

## 7. Epic 拆分（用于 Issue / Milestone）

1. `EPIC-01` 估值可靠性与降级机制（P0）
2. `EPIC-02` 今日决策层与动作风险分级（P0）
3. `EPIC-03` 执行约束与日志模型升级（P0）
4. `EPIC-04` T+1 确认与交易流水（P1）
5. `EPIC-05` 复盘引擎与不操作对照（P1）
6. `EPIC-06` 风险视图与策略版本化（P1）
7. `EPIC-07` 调度与推送系统（P2）
8. `EPIC-08` 多组合与年度报告（P2）
