# AGENTS.md（VectorControl 仓库执行规范）

本文件用于约束代码代理在本仓库的执行行为。优先级：用户当轮指令 > AGENTS.md > ROADMAP.md > docs/*。

## 1. 项目目标（阶段二）
- 多用户登录与数据隔离可用且可验证。
- 持仓页为主工作台，首页聚焦“资产总览 + 主动作 + 今日待办”。
- 交易链路逐步闭环（买入/定投/赎回/转换）。
- 接口口径统一，外部数据源失败可降级，页面不中断。

## 2. 开发铁律
1. 文档、注释、提交信息全部中文。
2. 文本编码统一 UTF-8（无 BOM）。
3. 每完成一个可验收小点就提交一次，禁止超大混合提交。
4. 提交前必须自测：
`frontend`: `npm run build`
`backend`: `python -m compileall app` + 最小接口烟测。
5. 代码变更后必须同步 `ROADMAP.md` 与相关文档（`README.md`、`docs/*`）。
6. `ROADMAP.md` 状态统一使用：`[ ]` 未完成、`[√]` 已完成。
7. `git status --short` 必须干净，不得夹带运行产物。

## 3. 架构边界
- 运行态真源：SQLite / Postgres（按部署环境配置）。
- `config/*.yaml` 仅用于初始化导入与导出备份，不作为运行态直接写入目标。
- 前端不得直接改 YAML，只能走后端 API。
- 后端分层：
`main.py` 负责应用装配与中间件。
`api/routers` 放路由。
`storage` 放数据访问。
`data_sources` 放外部源（必须超时 + 回退）。

## 4. 契约与 Gate
- 核心接口遵循“只增字段，不删字段，不改语义”。
- Gate-A：新机器 5 分钟可复现。
- Gate-B：核心契约稳定。
- Gate-C：外部源降级可用。
- Gate-D：VPS 部署闭环（Compose + Nginx + HTTPS/HTTP最小闭环 + 验收脚本）。

## 5. 停止条件
- 本轮指定目标完成后，立即停止并输出“本轮完成，等待下一指令”。
- 若发生阻塞，输出阻塞点与下一步最小动作后停止等待。

---

# ⚠️ VectorControl Agent 核心行为禁止条例 (2026-02-07 事故后修订)

## 1. 绝对地理边界禁止 (Physical Boundary)

* **禁止越域操作**：严禁对 `<local>\VectorControl` 及其明确关联的 worktree 目录（`-backend`, `-frontend`, `-ref`）之外的任何路径执行写入、重命名或删除操作。
* **禁止根目录扫描**：严禁对 `D:\` 根目录或其他非项目目录执行任何形式的目录遍历（`ls`, `dir`, `Get-ChildItem`）并基于结果执行逻辑判断。
* **禁止清理黑名单模式**：严禁使用“删除除 X 以外的所有文件”这种基于排除法的清理逻辑。所有删除指令必须**精确匹配**到具体的文件名或项目内的子目录名。

## 2. 破坏性指令熔断 (Destructive Command)

* **禁止静默删除**：严禁在未经过用户手动确认的情况下，对超过 5 个文件或任何非运行产物和开发代码（如 `.log`, `.tmp`, `__pycache__` 之外的文件）执行批量删除。
* **禁止强制进程关闭**：严禁通过脚本结束（`taskkill`, `Stop-Process`）非本项目拉起的后台进程，除非该进程名被明确记录在项目的运行 PID 文件中。
* **禁止全局环境修改**：严禁修改系统级环境变量、注册表或影响其他软件运行的全局配置文件。

## 3. 自动化与脚本安全 (Script Safety)

* **禁止使用模糊路径**：脚本中所有路径引用必须使用绝对路径或基于项目根目录的可靠相对路径，严禁使用 `./../*` 等可能导致逻辑溢出到根目录的模糊表达。
* **禁止危险参数组合**：在使用 `rmdir`, `git clean`, `shutil.rmtree` 等函数时，严禁将 `/s /q` 或 `-fd` 等强制递归参数应用于变量路径，除非该变量经过了严格的项目内前缀校验。
* **禁止静默失败重试**：如果删除操作因权限被拒绝，严禁尝试通过权限提升或终止外部进程来“强行完成”，必须立即停止并报告阻塞。

## 4. 事故溯源要求 (Audit Requirement)

* **先备份后结构调整**：在进行大规模目录重排（如 Worktree 迁移）前，必须先将受影响的配置文件备份至 `.runtime/backup`。
* **日志强制记录**：所有涉及文件系统结构变更的操作（Move/Rename/Delete），必须在控制台清晰打印：`[ACTION] <源路径> -> <目标路径>`。

---

### **给本地 Agent 的执行指令：**

> “请将上述条例完整写入项目根目录的 `AGENTS.md`。自此时起，你的所有文件操作权限已受到此条例约束。任何违反上述‘绝对禁止’项的操作都将被视为最高等级的失职。”
