# AGENTS.md（VectorControl 仓库级执行规范）

本文件是仓库内自动化 Agent 的最高执行规范（仅次于用户当轮明确指令）。
优先级：`用户当轮指令 > AGENTS.md > ROADMAP.md > docs/*`

## 1. 分支与发布（强制）

- `dev`：唯一开发分支（单人开发与集成都在此进行）。
- `main`：唯一发布分支（仅接收可上线版本）。
- 禁止在 `main` 直接做功能开发。

## 2. 版本号规则（强制）

- 每次发布到 `main` 必须携带版本号。
- 版本号格式：`vMAJOR.MINOR.PATCH`（例如 `v1.0.3`）。
- 发布动作至少包含：
  - 创建并推送 Git Tag（必须）
  - 在提交信息或发布说明中写明版本号（必须）
- `dev` 分支提交可以不带版本号。

## 3. main 发布说明规则（新增，强制）

- 任何“带版本号”的 `main` 发布提交，提交信息不得只写“合并 dev”。
- 发布提交必须明确写出本次改进内容，至少包含以下 3 项：
  - `新增`：新增了什么能力
  - `修复`：修复了什么问题
  - `优化`：优化了什么体验或性能
- 推荐格式：
  - 标题：`发布: vX.Y.Z - <一句话摘要>`
  - 正文：
    - `新增: ...`
    - `修复: ...`
    - `优化: ...`

## 4. main 推送后默认动作（强制）

- 当 `main` 有新发布提交后，默认执行 SSH 上云部署流程（VPS）。
- 默认流程：
  1. SSH 登录 VPS
  2. 拉取 `origin/main`
  3. 执行部署更新脚本
  4. 执行 Gate-D 验收脚本
  5. 输出可访问地址与验收结果

## 5. VPS 测试账号与数据（强制）

- 每次 `main` 发布后，VPS 必须准备可登录测试账号与样例数据。
- 默认测试账号：
  - 用户名：`admin`
  - 密码：高强度密码（禁止弱口令）
- 必须保证测试账号可看到持仓数据（真实持仓或明确样例持仓）。

## 6. 开发铁律

- 所有文档、注释、提交信息使用中文。
- 所有文本文件编码统一为 `UTF-8 无 BOM`。
- 改动遵循“最小闭环”，禁止顺手大改。
- 提交前必须通过：
  - 前端：`npm run build`
  - 后端：`python -m compileall app`
- 代码改动必须同步更新文档与 `ROADMAP.md`。

## 7. Gate 约束

- Gate-A：新环境 5 分钟可复现基本闭环。
- Gate-B：接口契约稳定且联调通过。
- Gate-C：外部数据源失败可降级且可解释。
- Gate-D：VPS 部署闭环（Compose + Nginx + HTTPS/HTTP 最小闭环 + 验收脚本）。

## 8. 禁止行为

- 禁止未授权的破坏性命令（如 `git reset --hard`、全仓 `git clean -fdx`）。
- 禁止无关重构、无关美化、无关依赖升级。
- 禁止在未验收通过时宣称完成。

## 9. 停止条件

- 当轮目标完成并验收通过：输出“本轮完成，等待下一指令”并停止。
- 若遇阻塞：输出阻塞点与下一步最小动作后停止。