# AGENTS.md（VectorControl 仓库级执行规范）

本文件用于约束代码代理在本仓库的行为。默认所有开发都要遵守本文件；若用户当轮指令与本文件冲突，以用户当轮指令优先。

## 1. 项目目标（阶段二）
- 多用户登录与数据隔离可用且可验证。
- 持仓页是主工作台，首页聚焦任务流（资产总览 + 主CTA + 今日待办）。
- 交易链路逐步打通（买入/定投/赎回/转换）。
- 数据口径统一、失败可降级、页面不中断。

## 2. 当前协作模式
- 当前默认模式：总控单人开发（`dev` 分支推进，`main` 保持稳定）。
- 若恢复并行：后端与前端分支先各自完成，再由总控按 Gate 规则合并。

## 3. 开发铁律（必须执行）
1. 文档、注释、日志、错误信息、提交信息全部中文。
2. 文本编码统一 UTF-8（无 BOM）。
3. 每完成一个小任务点就提交一次，禁止超大混合提交。
4. 每次提交前必须自测：
   - 前端：`npm run build` 通过。
   - 后端：`python -m compileall app` 通过，并执行最小接口烟测。
5. 代码变更后必须同步更新 `ROADMAP.md` 与相关文档（`README.md`、`docs/*`）。
6. `ROADMAP.md` 状态统一使用：`[ ]` 未完成，`[√]` 已完成。
7. 提交前 `git status --short` 必须干净，不得夹带运行产物。
8. 禁止提交：`__pycache__/`、`*.db`、`node_modules/`、临时日志。

## 4. 架构与边界
- 运行态数据真源：SQLite。
- YAML（`config/*`）仅用于导入初始化/导出备份，不作为运行态写入目标。
- 前端不得直接改 YAML，只能走 API。
- 后端分层边界：
  - `main.py` 只负责应用装配/中间件/路由注册。
  - 路由在 `backend/app/api/routers/`。
  - 数据访问在 `backend/app/storage/`。
  - 外部源在 `backend/app/data_sources/`，必须有超时和回退。

## 5. 契约冻结（Gate-B）
- 核心接口遵循“只增字段，不删字段，不改语义”：
  - `/api/estimate`
  - `/api/advice`
  - `/api/actions`
  - `/api/report/daily`
  - `/api/config`
  - `/api/holdings`
- `/api/estimate` 每只基金必须包含：`status`、`reason`、`source`、`asof`。

## 6. 多用户隔离硬要求
- 除 `/api/health` 与 `/api/auth/*` 外，所有 `/api/*` 必须登录态。
- 所有业务读写按 `user_id` 强制隔离。
- 前端本地缓存键必须带 `user_id`，切账号先清缓存再拉取新数据。
- 未登录或无效会话返回 `401`，错误信息中文。

## 7. 设计与交互硬要求
- 不出现开发术语文案，全部是用户语言。
- 图表必须有基准虚线（`0%` 或成本线）。
- 涨跌遵循“涨红跌绿”，且同时提供 `+/-` 或方向语义（不能只靠颜色）。
- 关键流程必须具备三态：加载、成功、失败。
- 空状态必须提供主动作 CTA。
- 数字展示使用等宽数字（`tabular-nums`）。

## 8. Gate 执行顺序（优先级）
1. Gate-A 可复现：新机器 5 分钟可跑通核心闭环。
2. Gate-B 契约稳定：接口不破坏，主路径回归通过。
3. Gate-C 降级可用：外部源部分失败时显示覆盖率与失败原因，且页面不中断。
4. Gate-D 部署闭环：VPS 可访问、HTTPS 可用、数据库不暴露公网、可回滚。

## 9. 本仓库默认任务顺序（总控）
1. 先完成治理与口径一致（登录隔离、契约、降级）。
2. 再做首页/持仓主路径体验。
3. 再做交易与消息闭环。
4. 最后做性能埋点与验收脚本完善。
5. 发布前必须通过 Gate-D（生产部署脚本 + 验收脚本）。

## 10. 停止条件（必须遵守）
- 若本轮指定任务全部完成：停止继续开发，输出“本轮完成，等待下一指令”。
- 若阶段任务全部完成：停止继续开发，输出“阶段任务完成，等待下一指令”。
- 若发现工作区出现异常改动且无法确认来源：立即暂停并上报。

## 11. 提交信息规范（中文）
- 推荐前缀：`功能:`、`修复:`、`文档:`、`测试:`、`重构:`。
- 示例：`功能: 完成首页任务流与底部导航最小可用`。
