# Postmortem: 测速 Not Found 与设置中心白屏双故障（后端视角）

- **故障编号**: P0-20260208-01
- **发现时间**: ~2026-02-08
- **影响范围**: 
    - **测速 Not Found**: 用户无法使用网络测速功能。直接影响设置中心的网络质量评估功能。
    - **设置中心白屏**: 用户无法访问任何设置项，包括网络测速、通知配置等，严重影响可用性。
- **根因分析**:
    - **测速 Not Found (后端侧)**:
        1.  **路由缺失**: 后端服务 `VectorControl/backend` 未提供对历史 API 路径 (`/api/network-benchmark/*` 及其 `_` 变体) 的兼容，导致前端直接调用这些旧路径时返回 404。
        2.  **网关配置不全**: Nginx 反向代理层 (`VectorControl/deploy/nginx/site.conf`) 缺少针对旧路径到新标准路径 (`/api/settings/network-benchmark/*`) 的重写规则，未能屏蔽后端的接口路径变更。
    - **设置中心白屏 (前端侧)**:
        - 初步判断为前端组件 `SettingsDrawer.jsx` 在处理 API 请求异常（如 404）时，缺少充分的错误兜底逻辑，导致 React 组件树渲染崩溃。详细根因待前端补充。
- **修复提交**:
    - **后端修复**: 
        - 提交哈希: (待总控根据 `agent_comms.md` 关联确认，与测速 Not Found 修复任务对应)
        - 修复内容: 
            - 在 `backend/app/api/routers/settings.py` 中增加了对 `/api/network-benchmark/*` 和 `/api/network_benchmark/*` 的兼容路由。
            - 在 `deploy/nginx/site.conf` 中增加了 `307` 临时重定向规则，将旧路径统一转发至新路径。
    - **前端修复**: 
        - 提交哈希: `f36d94f` (已合入 `dev@943c7a9`)
        - 修复内容: (待前端补充)
- **回归范围与证据**:
    - **后端回归**:
        - **范围**: API 路由兼容性、Nginx 重写规则、接口响应码。
        - **证据**:
            - 详见 `docs/Gate-D测速NotFound后端证据.md` 中的本地 TestClient 验收记录。
            - 生产环境需补录 `curl` 命令对 `/api/settings/network-benchmark/latest` 和 `/api/network-benchmark/latest` 的调用验证。
    - **前端回归**:
        - **范围**: 设置中心抽屉打开、测速模块渲染、接口异常兜底。
        - **证据**:
            - 详见 `docs/Gate-D设置中心测速前端证据.md`。
            - **[待前端补充]** 需要提供生产环境实机截图，验证白屏已修复且错误提示正常。
- **预防项**:
    1.  **强化契约驱动**: 未来的接口路径变更需先更新 `docs/接口契约.md`，并由总控评审影响面。
    2.  **增强网关兼容性**: 制定网关层路由兼容性策略，对于非破坏性变更，优先在网关做重写/兼容，减少对后端服务的直接修改。
    3.  **完善前端异常兜底**: 全局增强 API 请求的错误处理，组件级增加 `ErrorBoundary`，防止单点接口失败导致整页白屏。
    4.  **自动化回归测试**: 将覆盖兼容路由和异常场景的测试用例加入 CI/CD 流程。

## 待办清单

- **[ ] [前端Agent1]**: 补充前端侧的根因分析和修复内容详情。
- **[ ] [前端Agent1]**: 在 `docs/Gate-D设置中心测速前端证据.md` 中补齐生产环境的实机截图，证明白屏问题已修复且错误状态有正确兜底。
- **[ ] [总控]**: 关联后端的修复提交哈希。
- **[ ] [总控]**: 验收前端提供的实机截图，确认双故障均已关闭。
- **[ ] [协同]**: 归档本 Postmortem 文档。
