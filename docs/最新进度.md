# 最新进度（发布链路、OpenSpec 接入与优化基线收敛）
更新时间：2026-03-08 01:54:05

## 1. 本轮已完成

### 1.1 缺失文件补齐
- 已补齐 `frontend/src/hooks/useAppNavigation.js` 与 `frontend/src/hooks/useAppNavigation.test.jsx`，修复前端导航状态收敛后的缺失文件问题。
- 已补齐 `scripts/deploy_prod.sh`、`scripts/update_prod.sh`、`scripts/backup_db.sh`，让 `deploy/` 目录具备仓库内可执行的最小部署、更新与备份入口。

### 1.2 发布门禁脚本落地
- 已补齐 `scripts/check_docs_gate.py`、`scripts/check_release_preflight.py`、`scripts/check_main_release.py`、`scripts/check_secrets_leak.py`、`scripts/check_gate_d.py`。
- 已将 `scripts/check_release_message.py` 从英文 Conventional Commits 校验收敛为仓库要求的中文提交前缀与 `发布: vX.Y.Z - ...` 四段发布说明校验。
- `python scripts/check_release_preflight.py` 当前默认包含：`python scripts/check_docs_gate.py --strict`、`python scripts/check_secrets_leak.py`、`python -m compileall backend/app`、`PYTHONPATH=backend python -m pytest -q ...`、`npm --prefix frontend run test:run`、`npm --prefix frontend run build`；其中 docs gate / secrets leak / compileall 已做并发启动优化并输出耗时。
- 当前主仓已再次实跑通过：`python scripts/check_docs_gate.py --strict` 与 `python scripts/check_release_preflight.py`。
- `prod` 正式更新部署已通过：在 `/opt/vectorcontrol` 实跑 `sudo bash scripts/update_prod.sh`，并修复生产链路中的 `deploy/nginx/Dockerfile` 复制路径、`deploy/docker-compose.prod.yml` 对旧 `site.rendered.conf` 的挂载，以及 `deploy/nginx/nginx.conf` 中 brotli 启动阻塞。
- `prod` 更新后的实机复验已通过：`docker compose -f deploy/docker-compose.prod.yml ps`、`curl http://127.0.0.1/api/healthz`、`python3 scripts/check_gate_d.py --base-url http://127.0.0.1` 已再次返回通过结果。
- `docs/Gate-D验收证据-20260307.md` 已回填正式更新部署结果；新增证据输出 `docs/evidence/gate-d-20260307/deploy-docker-compose-ps-20260307.txt`、`docs/evidence/gate-d-20260307/deploy-healthz-20260307.json`、`docs/evidence/gate-d-20260307/deploy-gate-d-20260307.txt`。

### 1.3 hooks / workflows / 文档收敛
- `.githooks/commit-msg` 已对齐 `python scripts/check_release_message.py`。
- `.githooks/pre-push` 已对齐 `python scripts/check_secrets_leak.py` + `python scripts/check_docs_gate.py --strict`。
- `.github/workflows/docs-gate.yml` 已对齐严格模式与 `--diff-base` 校验。
- `.github/workflows/release-consistency.yml` 已对齐 `python scripts/check_main_release.py --commit HEAD --check-remote-tag --remote origin`。
- `README.md`、`docs/架构说明.md`、`docs/Git工作流.md`、`docs/开发规范.md`、`docs/部署与运行.md`、`AGENTS.md`、`ROADMAP.md` 已继续按当前运行事实同步。
- `docs/Gate-D验收证据模板.md` 与 `docs/Gate-D设置中心测速前端证据.md` 已重写为当前 `HTTP + Nginx + backend + SQLite` 基线模板，并去除过期的 HTTPS / Postgres / 历史截图口径。
- `docs/Gate-D测速NotFound后端证据.md` 与 `docs/Postmortem-测速NotFound与设置中心白屏-后端.md` 已同步收敛到 `deploy/nginx/site.http.conf` 与当前 HTTP 基线，避免继续引用过期的 `site.conf` / HTTPS 口径。
- 双故障关闭留档已按 `docs/P0线上故障排查SOP.md` 收敛为可引用证据链：后端证据、前端证据模板、postmortem 与正式更新部署复验证据已全部归档，当前阻塞项已清空。

### 1.4 OpenSpec 变更驱动入口落地
- 已新增仓库内 `openspec/` 骨架，并创建首个活跃变更：`openspec/changes/vectorcontrol-optimization-phase1/`。
- 当前变更已补齐：
  - `.openspec.yaml`
  - `proposal.md`
  - `design.md`
  - `tasks.md`
  - `specs/delivery-governance/spec.md`
  - `specs/frontend-performance-baseline/spec.md`
- 本轮开始采用“先变更文档，再改代码/脚本/部署”的仓库内 OpenSpec 流程。

### 1.7 第二轮已落地的低风险优化切片
- 前端：`SettingsDrawer` 已从“打开抽屉即全量预取”收敛为按区块按需加载；相关回归已通过 `src/components/SettingsDrawer.test.jsx`。
- 前端：`frontend/src/utils/metrics.js` 已改为缓冲后批量刷盘，降低高频 `recordMetric()` 对 `localStorage` 的同步读写压力；`src/utils/metrics.test.js` 已覆盖缓冲、隐藏页刷盘与 120 条上限行为。
- 后端：`estimate_snapshot` 读路径已补齐最小安全索引，用于支撑 `estimate/charts/benchmark/system` 共享快照读取。
- 后端：`charts.py` 已抽出共享日级聚合 bundle，并在 `returns_history`、`cumulative_returns`、`home_dashboard` 间复用缓存与聚合结果。
- 后端：`diagnostic_text` 已补充 SQLite 可读排障块，复制诊断文本时可直接看到 `DB Path / DB Exists / DB Size / Journal Mode / Busy Timeout / Page Size / Page Count / Freelist Count`，便于无界面环境下快速定位持久化问题。
- 后端：`get_sqlite_observability_snapshot()` 已补充 `db_dir`、文件修改时间、`synchronous`、`wal_autocheckpoint_pages` 以及 `derived.lock_risk / wal_state / free_ratio_pct / observations`，将“锁风险 / WAL 状态 / 目录可写性 / 空页偏高”整理为可复制的最小排障提示。

### 1.9 第二轮继续落地的切片
- 前端：首页已改为“摘要优先、轻量 SVG 图表优先、扩展基准面板延后加载”，`App.jsx` 主路径已不再懒挂载 `ReturnsChart` / `BenchmarkComparison` 两个 ECharts 入口，首页首屏仅保留 `SummaryCards`、`DataStatusBanner`、`PortfolioReturnsPanel` 与延后加载的 `BenchmarkComparisonPanel`。
- 前端：已删除 `frontend/src/utils/echartsCore.js`、`frontend/src/components/ReturnsChart.jsx`、`frontend/src/components/BenchmarkComparison.jsx` 三个过渡文件，并从 `frontend/package.json` / `frontend/package-lock.json` 移除 `echarts`、`echarts-for-react`；当前前端源码检索与 analyze 输出中都已不再存在独立 ECharts runtime。
- 前端：交易中心已从 `App.jsx` 主路径抽离为懒加载 `TradeCenter.jsx`，并在 `vite.config.js` 中新增 `trade-antd` 分块，让交易页的 `DatePicker/Table/Tooltip/InputNumber` 重依赖从首页主包继续外移。
- 前端：`SettingsDrawer` 已将 `Drawer width` 弃用用法收敛为 `size`，当前 `SettingsDrawer.test.jsx` 不再打印 Ant Design `width is deprecated` 噪音。
- 前端：`assertSettingsSchema()` 已在测试环境跳过，`SettingsDrawer.test.jsx` 当前已不再输出开发态 schema 警告噪音。
- 前端：`新增持仓` 与 `提醒规则` 两段表单已从 `App.jsx` 主路径抽离为懒加载 `HoldingsCreatePanel.jsx` / `ReminderRulesPanel.jsx`；当前 analyze 中 `index` 已进一步收敛到约 `101.51 kB`（gzip `32.07 kB`）。
- 前端：`LoginPanel.jsx` 已改为按需懒加载，当前 analyze 中 `LoginPanel` 独立 chunk≈`2.14 kB`（gzip `1.22 kB`），`index` 进一步收敛到约 `99.90 kB`（gzip `31.38 kB`）。
- 前端：`App.jsx` 外壳、`SideNav.jsx` 与 `PortfolioReturnsPanel.jsx` 已去除首页主路径上的 Ant Design `Layout/Spin/Input.Search/Segmented/Sider` 依赖；当前 analyze 中 `antd-vendor` 进一步收敛到约 `997.66 kB`（gzip `295.57 kB`），`index` 保持在约 `100.26 kB`（gzip `31.37 kB`）。
- 前端：在首页主路径基本脱离 Ant Design 之后，`vite.config.js` 已取消对 `antd` 的统一 vendor 强切，analyze 当前不再出现 `500 kB` 以上 chunk 警告；当前 chunk 基线转为 `Table`≈`222.73 kB`、`index-DibHFOCK`≈`206.27 kB`、`TradeCenter`≈`198.84 kB`、`react-vendor`≈`142.33 kB`，后续继续按页面/能力收敛。
- 前端：`TopToolbar.jsx` 已改为延后加载，`App.jsx` 也补齐了 `formatDateTime` / `StateShowcase` 的运行时缺失引用，并新增 `App.test.jsx` 作为最小渲染 smoke；当前 analyze 中 `TopToolbar` 独立 chunk≈`34.81 kB`，新的主入口 chunk≈`15.50 kB`，不再存在单个超大 vendor chunk。
- 协同：`scripts/perf_smoke.py` 已落地并跑出首份本地页面基线文件 `.perf/perf_smoke/latest.json`，当前记录：首页 `1263ms`、基金详情页 `899ms`、系统状态页 `852ms`；基线文件默认不入库。
- 前端：`TopToolbar.jsx` 已进一步去除对 Ant Design 组件的依赖，当前 `TopToolbar` chunk≈`17.05 kB`（gzip `5.88 kB`）；analyze 仍不再出现 `500 kB` 以上 chunk 警告，新的热点转为 `index-DQ3zwGHZ`≈`231.68 kB`、`index-Dfh634J-`≈`206.61 kB`、`TradeCenter`≈`198.78 kB`。
- 前端：已通过本地临时代理抓到 `recordMetric()` 样本，当前最小交互基线已包含：
  - `应用打开`
  - `首屏加载开始`
  - `底部Tab切换(tab=home)`
  - `设置中心打开`
  - `底部Tab切换(tab=trade)`
  这意味着前端 bundle 优化 phase2 中“结合现有 `recordMetric()` 输出整理首页、交易入口、设置中心关键交互基线”已补齐。
- 前端：基金详情已具备独立路由 `/fund/:fund_id` 与“左侧净值走势/交易记录 + 右侧持仓详情/最新净值”的高信息密度布局；`FundDetailPage.test.jsx`、`useAppNavigation.test.jsx`、`api.test.js` 已组成最小回归组合。
- 前端：`FundDetailPage.jsx` 中的 `Spin tip` 与 `Statistic valueStyle` 已按 Ant Design 6 推荐用法收敛为 `description` / `styles.content`，基金详情页回归测试不再打印对应 deprecation 警告。
- 前端：`HoldingsTable.jsx` 现已在保留列显示/隐藏能力的基础上新增“零份额持仓隐藏”开关，并由 `HoldingsTable.test.jsx` 覆盖 50+ 列表、编辑与空仓隐藏行为。
- 协同：通知通道 `test_message` 的统一动作结果模型（`NotifierActionResult` / `NotifierActionError`）已在 `docs/接口契约.md`、`backend/app/api/routers/settings.py` 与设置中心渲染侧形成 SSOT，前端无需再按 provider 特判。
- 后端：`request_id` 中间件已补充 `X-Server-Elapsed-Ms` 响应头，形成最小请求时长信号，并由 `test_request_id_middleware_smoke.py` 覆盖。
- 后端：`request_id` 中间件现已进一步补齐“请求完成结构化日志”信号，稳定输出 `method/path/status_code/request_id/server_elapsed_ms`，用于和 `X-Server-Elapsed-Ms` 互补定位热点请求。
- 后端：应用启动流程已从 `FastAPI.on_event("startup")` 收敛到 `lifespan`，当前 preflight 中与应用装配相关的弃用警告已缩减为仅剩 `python_multipart` 上游提示。
- 后端：`/api/system/status` 与 `/api/system/diagnostics` 现已附带最近请求摘要（最多 5 条），并在 `diagnostic_text` 中输出 `time/method/path/status/elapsed/request_id`，便于直接观察热点接口。
- 后端：`openspec/changes/backend-observability-phase2/tasks.md` 当前已全部勾选，第二轮后端观测 phase2 已完成阶段性收口。
- 后端：`FeishuSender` 现已补齐发送侧治理：同 webhook 最小频控、连续失败隔离、治理状态 TTL 清理与结构化治理日志；`test_notifier_feishu_sender_smoke.py` 已覆盖节流与隔离场景。
- 前端：`openspec/changes/frontend-bundle-optimization-phase2/tasks.md` 当前已全部勾选，第二轮前端 bundle phase2 已完成阶段性收口。
- 后端：`/api/transactions/import` 的幂等冲突语义已修正为返回 `409`，并与 `/api/transactions/import_json` 的标准 JSON 导入口径一起补入 `docs/接口契约.md`；`test_transactions_import_smoke.py` 已再次通过。

### 1.10 本轮新增验证结果
- 前端测试：`npm --prefix frontend run test:run` 已通过（49 passed）。
- 前端分析：`npm --prefix frontend run analyze` 已再次刷新；首页主路径与前端依赖树都已不再产出 ECharts runtime，当前仅剩 `antd-vendor`≈`960.94 kB`（gzip `301.14 kB`）、`index`≈`124.96 kB`（gzip `38.46 kB`）与 `SettingsDrawer`≈`37.51 kB`。这说明第二轮首页图表 bundle 收缩已完成“彻底移出前端运行时”阶段，下一轮主攻 `antd-vendor` 热点。
- 前端交易中心懒加载验证：`npm --prefix frontend run test:run` 继续通过（49 passed），`npm --prefix frontend run analyze` 当前产出 `index`≈`105.56 kB`（gzip `33.01 kB`）、`antd-vendor`≈`849.94 kB`（gzip `249.61 kB`）、`trade-antd`≈`159.42 kB`（gzip `51.28 kB`）、`TradeCenter`≈`16.35 kB`（gzip `5.19 kB`）；analyze 仍提示 `trade-antd -> antd-vendor -> trade-antd` 的循环分块告警，后续继续收紧 `manualChunks` 规则。
- 前端循环分块修复：将 `TradeCenter.jsx` 的 AntD 引用改成直达入口，并移除 `vite.config.js` 中对 `trade-antd` 的手工硬切后，analyze 已不再出现 `trade-antd -> antd-vendor -> trade-antd` 循环分块告警；当前 `index`≈`105.44 kB`（gzip `32.94 kB`），但 `antd-vendor` 回升到≈`1006.91 kB`（gzip `298.28 kB`），下一刀需继续在“消告警”和“压 vendor”之间找更优平衡。
- 后端请求时长信号与 SQLite 诊断验证：`python -m compileall backend/app && PYTHONPATH=backend python -m pytest -q backend/app/api/test_request_id_middleware_smoke.py backend/app/api/test_healthz_smoke.py backend/app/api/test_system_diagnostics_smoke.py` 已通过（9 passed）；`/api/system/status` 与 `/api/system/diagnostics` 当前已稳定返回 SQLite `db_dir`、`synchronous`、`wal_autocheckpoint_pages` 与 `derived.lock_risk / observations` 字段，并同步写入诊断文本。
- 治理脚本验证：已用 3 组样例提交消息验证 `scripts/check_release_message.py`，确认普通中文前缀提交可通过、英文 `fix:` 前缀会被阻断、带 `新增/修复/优化/文档` 四段的发布提交可通过。
- 主仓预检验证：`python scripts/check_release_preflight.py` 已在当前集成态通过，说明 docs gate、敏感信息扫描、后端 compileall/烟测、前端测试与 build 已重新对齐为绿色基线。

### 1.11 Telegram chat_id 自动发现闭环
- 后端：已新增 `POST /api/settings/notifications/telegram/discovery/secret` 与公开 inbound 路径 `POST /api/settings/notifications/telegram/inbound/{discovery_secret}`，支持“仅保存 `bot_token` -> 生成独立 secret -> 收到 Telegram update 后自动回写 `chat_id`”。
- 后端：`GET /api/settings/notifications/status` 已补齐 `telegram.bot_token_configured` 与 `telegram.discovery` 摘要；`GET /api/settings` / 审计日志对 `chat_auto_discovery_secret` 继续保持脱敏。
- 前端：设置中心已支持“生成自动发现地址”，并在通知诊断中展示最近自动发现的 `chat_id / time / chat_title`；`SettingsDrawer.test.jsx` 已新增对应回归。
- 验证：后端新增 smoke `backend/app/api/test_settings_telegram_chat_discovery_smoke.py`，并与 Telegram 凭据 / 通知状态 / Telegram test_message / 飞书 test_message 一起通过（24 passed）；前端全量测试已通过（53 passed）。
- `prod` 已完成正式部署与实测：通过 `Host: ` 的真实生产入口完成“注册测试用户 -> 保存空 `chat_id` -> 生成 secret -> 模拟 inbound -> `status` 读回”的闭环，证据已补入 `docs/evidence/gate-d-20260307/-20260307.json`。

### 1.12 GitHub 分支保护治理回补
- 远端 GitHub 分支保护已恢复：`dev` 现要求 `Docs Gate / docs-gate`，`main` 现要求 `Docs Gate / docs-gate` 与 `Release Consistency / verify-release`。
- 已通过 GitHub API 留档当前保护结果，证据位于 `docs/evidence/gate-d-20260307/github-branch-protection-dev-20260307.json` 与 `docs/evidence/gate-d-20260307/github-branch-protection-main-20260307.json`。
- `ROADMAP.md` 的最后未勾选项已关闭；当前仓库治理面只剩“运行时 version/commit 元数据仍可能为 unknown”这类非 ROADMAP 阻塞增强项。

### 1.13 prod HTTPS 恢复
- `deploy/docker-compose.prod.yml` 已重新挂载 `deploy/nginx/site.rendered.conf`；`scripts/render_nginx_site.py` 现按 `VC_ENABLE_TLS` 在 `site.http.conf` / `site.conf` 间切换并替换 `VC_DOMAIN`。
- `scripts/update_prod.sh` 与 `scripts/deploy_prod.sh` 已补齐：创建 `certbot` 目录、渲染运行态配置，并在 TLS 模式下校验证书存在。
- 已确认 `2026-03-08` 时 `vectorcontrol.tech` 解析到 ``（即 `prod` 公网地址），故障根因不是错机，而是：UFW 未放行 `443`、当前生产栈运行 HTTP 配置、证书目录为空。
- 已在 `prod` 上申请 `vectorcontrol.tech` / `www.vectorcontrol.tech` 的 Let's Encrypt 证书，证书有效期到 `2026-06-05`；并已放行 `443/tcp`。
- 已清理远端历史残留目录 `/opt/vectorcontrol/deploy/nginx/site.rendered.conf`，重新渲染 TLS 站点配置并成功重启生产栈。
- 已将 `scripts/update_prod.sh` / `scripts/deploy_prod.sh` 补强为“`docker compose up -d --build` 后等待 backend healthy”，避免刚重启完成即执行 Gate-D 时短暂命中 `502`。
- 公网验收已通过：`http://vectorcontrol.tech` 返回 `301 -> https://vectorcontrol.tech/`，`https://vectorcontrol.tech/` 返回 `200`，`https://vectorcontrol.tech/api/healthz` 返回 `{\"status\":\"ok\",\"service\":\"vectorcontrol-backend\"}`，`python scripts/check_gate_d.py --base-url https://vectorcontrol.tech` 通过。
- 证据已补齐：`docs/evidence/gate-d-20260307/https-http-redirect-20260308.txt`、`docs/evidence/gate-d-20260307/https-home-head-20260308.txt`、`docs/evidence/gate-d-20260307/https-healthz-20260308.json`、`docs/evidence/gate-d-20260307/https-gate-d-20260308.txt`、`docs/evidence/gate-d-20260307/https-docker-compose-ps-20260308.txt`、`docs/evidence/gate-d-20260307/https-cert-20260308.txt`。

### 1.14 首页工作台视觉升级（phase1）
- 参考 Ant Design 设计规范中的 Workbench / Data Display 层级，以及 GitHub Primer 的清晰层级与克制信息密度原则，对首页做了“一屏先看最关键指标”的视觉收敛。
- `frontend/src/App.jsx` 已新增首页概览区：将“最近同步 / 已纳入持仓”压入首页顶部，并将 `SummaryCards`、`DataStatusBanner`、数据质量条收进统一工作台容器。
- `frontend/src/components/SummaryCards.jsx` 已重构为更强层级的指标卡：增加 `kicker`、说明文案、图标容器与更明确的正负收益视觉语义。
- `frontend/src/components/DataStatusBanner.jsx` 已改为“主状态 + 时点 + 说明”三段式信息块；`frontend/src/components/PortfolioReturnsPanel.jsx` 已增加收益监控标题区、最新时点卡片与更明确的摘要面板层次。
- `frontend/src/main.jsx` 与 `frontend/src/index.css` 已同步收敛 Ant Design 主题圆角/主色，并补齐亮暗两套首页视觉样式。
- 验证已通过：`npm --prefix frontend run test:run`（`53 passed`）、`npm --prefix frontend run build`、`npm --prefix frontend run analyze`；当前 `dist/assets/index-*.css` 约 `101.44 kB`，主入口 JS 仍维持页面级拆分。

## 2. 当前真实运行口径
- 后端入口：`backend/app/main.py`
- 启动顺序：`load_all()` -> `init_db()` -> `sync_fund_catalog_from_config()`
- 数据库：SQLite，文件 `backend/data/app.db`
- 本地端口：后端 `21345`，前端 `5173`
- 生产基线：`deploy/docker-compose.prod.yml` + Nginx + backend + SQLite，当前以 HTTP 最小闭环为准

## 3. 当前正在推进的优化入口
- OpenSpec 变更入口：
  - `openspec/changes/vectorcontrol-optimization-phase1/`
  - `openspec/changes/frontend-bundle-optimization-phase2/`
- `openspec/changes/backend-observability-phase2/`
- `openspec/changes/telegram-chat-discovery-phase1/`
  - `openspec/changes/https-recovery-phase1/`
  - `openspec/changes/frontend-design-refresh-phase1/`
- 第一批收口主题：
  - 发布文档门禁范围与仓库声明对齐
  - Gate-D 模板与故障 SOP 对齐当前 HTTP + SQLite 基线
  - 前端新增正式分析入口（`npm --prefix frontend run analyze`）
  - 将现有 `recordMetric()`、`/api/system/diagnostics` 与 network benchmark 纳入性能基线流程
- 第二轮骨架已建立：
  - 前端 bundle 优化：聚焦 `antd-vendor`、页面级懒加载与 `recordMetric()` 基线对照
  - 后端观测优化：聚焦热点接口耗时记录、SQLite 热点查询与部署侧诊断增强

## 4. 当前剩余事项
- `openspec/changes/frontend-bundle-optimization-phase2/` 与 `openspec/changes/backend-observability-phase2/` 当前已完成并完成验证回写，本轮不再有待验证阻塞项。
- `ROADMAP.md` 当前已无未勾选条目；后续仅剩常规增强与发布元数据补强，不再阻塞本轮闭环。
- `/api/system/status` 与 `/api/system/diagnostics` 的运行时 `version/commit` 字段仍可能为 `unknown`，属于后续可独立补强的部署元数据问题。

## 5. 仍需注意的风险
- 生产 HTTP 基线虽已完成正式更新部署，但运行时 `version/commit` 元数据仍未进接口返回，后续若做发布治理增强需优先补齐。
- 文档门禁扩大后，后续任何脚本 / hooks / workflow 变更都必须同步更新更多核心文档。
- 生产 HTTPS 已恢复；当前与证书相关的剩余风险只剩“自动续期未自动化”。
- 前端性能优化已进入第二轮依赖级瘦身阶段，首页 ECharts runtime 已彻底出清，后续主攻 `antd-vendor` 与 `App.jsx` 顶层 Ant Design 依赖面收缩。
