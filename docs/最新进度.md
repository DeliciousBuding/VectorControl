# 最新进度（dev 分支）

更新时间：2026-02-06

## 1. 合并到 dev 之后已完成
- Gate-A/B/C 已固化并有对应脚本。
- 多用户会话登录与 `user_id` 硬隔离已落地。
- 持仓 CRUD、YAML 导入导出、估值覆盖率与降级可视化已落地。
- 前端持仓中心与基金详情主路径已完成（日期化表头、双行高密、排序三态、分组顺序、详情图周期切换与基准虚线）。
- 首页任务流、底部 5 Tab、消息中心与交易最小闭环已落地。
- 设计系统 Token 已落地，关键图表与数字展示口径已统一。

## 2. 本轮进行中（Gate-D：生产部署）
- 新增生产编排目录：`deploy/`
  - `docker-compose.prod.yml`
  - `backend/frontend/nginx` Dockerfile 与 Nginx 配置
  - `.env.example`
- 新增部署脚本：
  - `scripts/deploy_prod.sh`
  - `scripts/update_prod.sh`
  - `scripts/renew_cert.sh`
  - `scripts/backup_db.sh`
  - `scripts/check_gate_d.py`
- 后端新增健康接口：`/api/healthz`
- 文档已同步到 README、部署文档、开发规范、Git 工作流、接口契约。

## 3. 当前状态判断
- 代码与文档脚手架已齐全，可在有 Docker 与真实域名的 VPS 执行部署。
- 本地环境未安装 Docker，无法在当前机器完成真实 HTTPS 与容器级 Gate-D 实测。

## 4. 下一步（上线前必做）
1. 在 VPS 配置 `deploy/.env.prod`（域名/邮箱/token）。
2. 执行 `bash scripts/deploy_prod.sh`。
3. 执行 `VC_DOMAIN=<your-domain> python3 scripts/check_gate_d.py`。
4. 通过后将 ROADMAP 的 Gate-D 条目标记为完成。
