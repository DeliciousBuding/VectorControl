# 最新进度（发布链路、OpenSpec 接入与优化基线收敛）

更新时间：2026-08-11 18:45:18

## 1. 历史轮次（已归档）

历轮闭环（缺失文件补齐、发布门禁、hooks/workflows 收敛、OpenSpec 入口、前端 bundle/观测优化、Telegram chat_id 发现、HTTPS 恢复、phase1–18 视觉升级、登录白屏热修复）均已按轮次完成并验证，全文见 git log 与 `docs/ROADMAP完成归档.md`。当前事实以第 2–5 节为准。

发布门禁脚本 `scripts/check_docs_gate.py`、`scripts/check_release_preflight.py`、`scripts/check_main_release.py`、`scripts/check_secrets_leak.py` 已纳入全量门禁与 hooks 链路，任何脚本/hooks/workflow 变更需同步更新对应核心文档。

## 2. 当前真实运行口径

- 后端入口：`backend/app/main.py`
- 启动顺序：`load_all()` -> `init_db()` -> `sync_fund_catalog_from_config()`
- 数据库：SQLite，文件 `backend/data/app.db`
- 本地端口：后端 `21345`，前端 `5173`
- 生产基线：`deploy/docker-compose.prod.yml` + Nginx + backend + SQLite，当前以 HTTP 最小闭环为准

## 3. 当前正在推进的优化入口

- OpenSpec 变更入口：`openspec/changes/` 下 vectorcontrol-optimization / frontend-bundle-optimization / backend-observability / telegram-chat-discovery / https-recovery / frontend-design-refresh-phase1–18 / frontend-live-login-hotfix 各阶段目录
- 第一批收口主题：
  - 发布文档门禁范围与仓库声明对齐
  - Gate-D 模板与故障 SOP 对齐当前 HTTP + SQLite 基线
  - 前端新增正式分析入口（`npm --prefix frontend run analyze`）
  - 将现有 `recordMetric()`、`/api/system/diagnostics` 与 network benchmark 纳入性能基线流程
- 第二轮骨架已建立：
  - 前端 bundle 优化：聚焦 `antd-vendor`、页面级懒加载与 `recordMetric()` 基线对照
  - 后端观测优化：聚焦热点接口耗时记录、SQLite 热点查询与部署侧诊断增强

## 4. 当前剩余事项

- `openspec/changes/frontend-bundle-optimization-phase2/` 与 `openspec/changes/backend-observability-phase2/` 当前已完成并完成验证回写，本轮不再有待验证阻塞项。
- `ROADMAP.md` 当前已无未勾选条目；后续仅剩常规增强与发布元数据补强，不再阻塞本轮闭环。
- `/api/system/status` 与 `/api/system/diagnostics` 的运行时 `version/commit` 字段仍可能为 `unknown`，属于后续可独立补强的部署元数据问题。

## 5. 仍需注意的风险

- 生产 HTTP 基线虽已完成正式更新部署，但运行时 `version/commit` 元数据仍未进接口返回，后续若做发布治理增强需优先补齐。
- 文档门禁扩大后，后续任何脚本 / hooks / workflow 变更都必须同步更新更多核心文档。
- 生产 HTTPS 已恢复；当前与证书相关的剩余风险只剩"自动续期未自动化"。
- 前端性能优化已进入第二轮依赖级瘦身阶段，首页 ECharts runtime 已彻底出清，后续主攻 `antd-vendor` 与 `App.jsx` 顶层 Ant Design 依赖面收缩。
