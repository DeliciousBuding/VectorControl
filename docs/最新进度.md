# 最新进度（dev 分支）
更新时间：2026-02-10 18:30:58

## 1. 自合并到 `dev` 后的总体进度
- Gate-A/B/C 已完成并固化验收脚本。
- 多用户登录与 `user_id` 数据隔离已落地。
- 持仓主表、基金详情、多周期图表、状态口径统一已落地。
- 交易链路最小闭环、消息中心最小可用已落地。
- SIP 定投计划后端 v1 已落地：`/api/sip` 支持 CRUD + execute，路由统一接入 `app.storage.db`，并与 `init_db()` 统一建表迁移；新增 smoke 覆盖全链路、隔离与参数校验。
- SIP 定投计划前端 v1 已落地：`SettingsDrawer` 新增"定投计划（SIP）"管理面板，对接 `/api/sip` 并支持列表/新增/编辑/启停/执行记录/删除；回归新增 `SettingsDrawer` SIP 用例，`npm --prefix frontend run test:run -- src/components/SettingsDrawer.test.jsx` 与 `npm --prefix frontend run build` 已通过。
- Windows 本地一键启动脚本已落地：新增 `scripts/dev_up.ps1` 和 `scripts/dev_down.ps1`，支持自动创建虚拟环境、安装依赖、启动后端（uvicorn 21345）和前端（vite 5173），并包含健康检查；`docs/部署与运行.md` 已新增 1.1.5 节引用（已提交 `ddbb0f0`，待集成）。
- 发布预检脚本 Windows 终端中文支持已落地：`scripts/check_release_preflight.py` 新增 Windows 控制台 UTF-8 支持（自动设置代码页 65001），避免中文输出乱码；新增 `--selftest` 自测功能（已提交 `ebb28f9`，待集成）。
- Secrets Leak Guard v1.1 已落地：`scripts/check_secrets_leak.py` 扩展常见密钥形态扫描（AWS/Google/GitHub/私钥），低误报优先，并新增 `--selftest` 自测功能，自测验证通过。
- 系统状态面板 v2 已落地：`SettingsDrawer` 复制状态功能新增 `page_url` 和 `user_agent` 字段，便于线上排障定位页面和环境；测试和构建通过。
- 收益曲线体验 v3 已落地：`PortfolioReturnsPanel` 新增摘要面板，显示累计收益、最大回撤、近30天波动率及口径提示；测试和构建通过。
- 收益曲线体验 v4 已落地：`PortfolioReturnsPanel` 增强空状态提示，显示可解释说明和下一步建议；测试和构建通过（异常点 marker 待后端提供 anomalies 字段）。
- 本地验收数据种子已落地：新增 `scripts/dev_seed_demo.py` 演示数据写入脚本，支持二次确认和 dry-run，配套文档 `docs/开发环境演示数据种子.md`。
- 设计系统 Token 与首屏性能门禁已落地。
- 基金搜索闭环已增强：`/api/funds/search` 与 `/api/funds/suggest` 在本地目录无命中时会回源东方财富基金检索，并将命中结果自动入库（`fund_catalog/fund_master/fund_alias`），后续可直接查询详情。
- 全局搜索跳转已优化：点击搜索结果时，若基金不在当前持仓则自动跳转"基金中心"并加载该基金详情；持仓内基金保持原有持仓页跳转行为。
- 基准对比前端 UI v1 已落地：首页新增"基准对比"面板，对接 `GET /api/benchmark/list` 与 `GET /api/benchmark/comparison`。
- `unknown` 判定规则已固化：当 `benchmark_return=null` 或 `data_status.status!=confirmed` 时仅展示 `unknown`，不显示"跑赢/跑输"。
- 基准对比最小回归已补齐：新增 `frontend/src/components/BenchmarkComparisonPanel.test.jsx` 覆盖 unknown 与 confirmed 两种渲染分支。
- 前端飞书接入文档已落地：新增 `docs/飞书接入手册-前端.md`，明确设置入口、字段口径、权限边界、排障与回滚步骤。
- Telegram 通道接入文档已补齐：新增 `docs/telegram/Telegram通道配置与排障.md`，沉淀 chat_id 获取、凭据安全与常见错误排查。
- 设置中心 Telegram 通道配置已补齐：支持编辑基础参数，并提供"更新凭据"入口（bot_token/chat_id 走独立接口更新，不随 settings 保存请求回传）。
- Telegram 通道测试消息闭环已补齐：新增 `POST /api/settings/notifications/telegram/test_message`，用于本地/联调一键验证凭据与 chat_id 是否可用（响应不回显 bot_token）。
- 飞书通道测试消息闭环已补齐：新增 `POST /api/settings/notifications/feishu/test_message`，用于本地/联调一键验证 webhook 是否可用（响应不回显 webhook_url）。
- 设置中心敏感字段脱敏已落地：`GET /api/settings` 对 `notifications.feishu.webhook_url` 与 `notifications.telegram.bot_token` 回显 `"<REDACTED>"`，避免前端/录屏/日志拿到明文；`PUT /api/settings` 回传 `"<REDACTED>"` 会被忽略避免覆盖，凭据更新请走独立接口。
- 通知诊断只读状态已补齐：新增 `GET /api/settings/notifications/status`（只读脱敏，返回 enabled/credential_configured/last_test_summary，第一版允许 last_test_summary=null），用于设置中心"通知诊断面板"聚合展示。
- 飞书 webhook 治理 v1 已落地：`PUT /api/settings/notifications/feishu/webhook` 严格校验仅允许 `https` 且 host 必须为 `open.feishu.cn`；feishu sender 日志脱敏不输出 webhook 明文。
- Gate-D 前端测速证据文档已补齐：新增 `docs/Gate-D设置中心测速前端证据.md`，沉淀本地回归结果与实机截图补录清单。
- 发布治理新增"文档门禁"：`main` 更新前必须全量巡检并完善核心文档，发布说明新增 `文档` 小节。
- 文档治理继续收敛：补齐文档 SSOT 矩阵、部署文档发布前检查清单、接口契约中的设置与测速接口定义。
- 文档门禁自动化已落地：新增 `scripts/check_docs_gate.py`，并更新 `.gitmessage-zh.txt` 为 `新增/修复/优化/文档` 四段模板。
- 文档门禁 CI 化已落地（本地）：新增 `.githooks/pre-push`，push 前自动执行 `python scripts/check_docs_gate.py --strict`。
- 文档门禁流水线化已落地（云端）：新增 `.github/workflows/docs-gate.yml`，PR 到 `dev/main` 自动执行严格校验（含 diff-base）。
- 分支保护固化脚本已落地：新增 `scripts/branch_protection.py`，支持一键 `check/apply` `dev/main` 分支保护基线。
- 发布说明校验已落地：新增 `scripts/check_release_message.py`，`commit-msg` 钩子自动校验发布标题版本号与 `新增/修复/优化/文档` 四段。
- 发布一致性校验已落地：新增 `scripts/check_main_release.py` 与 `.github/workflows/release-consistency.yml`，校验 `main` 发布提交与 Tag 一致性。
- Gate-D 验收证据模板已落地：新增 `docs/Gate-D验收证据模板.md`，覆盖 HTTPS、回滚、巡检留档字段。
- 架构决策记录（ADR）已落地：新增 `docs/架构决策记录.md`，固化关键取舍与边界共识。
- ROADMAP 已进一步收敛：新增 P0.5（状态解释能力与系统自省），把 `data_status` 与 `/system/status` 作为下一阶段优先任务。
- P0.5 已落地第一步：后端新增 `GET /api/system/status`，返回版本号、commit、估值快照、基金同步任务与对账能力状态。
- P0.5 前端页已落地：新增 `/system/status`（我的页入口 + 路径直达），支持查看系统状态与手动刷新。
- P0.5 `data_status` 第一阶段已落地：`/api/estimate`、`/api/holdings*`、`/api/funds/*`、`/api/actions` 已统一返回 `status/asof/note`。
- P0.5 页面解释第一阶段已落地：首页/持仓/交易/基金中心已显示"数据口径条"（状态+时点+说明，含 Tooltip）。
- 接口契约已同步：`docs/接口契约.md` 增加 `/api/system/status` 出参说明。
- ROADMAP 已再次瘦身：`ROADMAP.md` 仅保留当前待办，已完成项迁移至 `docs/ROADMAP完成归档.md`。
- ROADMAP 已固化"全局发布门禁"：每次 `main` 更新必须完成文档全量巡检与必要完善，并通过 `check_docs_gate` 与 `check_main_release`。
- P0 线上故障排查 SOP 已落地：新增 `docs/P0线上故障排查SOP.md`，统一"测速 Not Found + 设置中心白屏"排查路径、验收标准与复盘模板。
- 文档门禁范围已扩展：`check_docs_gate.py` 与发布检查清单已纳入 `docs/P0线上故障排查SOP.md`。
- 分支保护脚本已增强：`scripts/branch_protection.py` 支持多必需状态检查（`--required-contexts`），可分别校验 `Docs Gate` 与 `Release Consistency`。
- 分支保护脚本已增强到分支级口径：支持 `--main-required-contexts/--dev-required-contexts`，可实现 `dev` 与 `main` 不同必需状态检查。
- 分支保护鉴权链路已增强：`branch_protection.py` 支持 `GITHUB_TOKEN/GH_TOKEN` 优先，缺失时自动回退 `gh auth token`。
- 文档门禁脚本已增强：`check_docs_gate.py` 新增"文档范围一致性校验"，自动检查 `AGENTS/ROADMAP/开发规范/Git工作流/部署与运行` 是否同步覆盖核心文档清单。
- 文档门禁脚本再次增强：新增 `.githooks/*` 与 `.github/workflows/*` 关键门禁文件存在性与关键命令校验，避免"文档齐全但门禁失效"。
- 架构与流程文档已同步：`AGENTS.md`、`docs/架构说明.md`、`docs/Git工作流.md`、`docs/部署与运行.md` 均已明确门禁文件完整性校验要求。
- 发布前预检脚本已落地：新增 `scripts/check_release_preflight.py`，默认串行执行文档门禁严格模式、后端 compileall、前端 build。
- 发布前预检脚本本地已验证通过：`check_release_preflight.py` 三步均通过；在 Windows 环境支持 `npm -> npm.cmd` 自动回退。
- 文档门禁新增更新时间校验：`check_docs_gate.py` 现对 `ROADMAP.md`、`docs/架构说明.md`、`docs/最新进度.md` 强制检查 `更新时间：2026-02-08 16:50:28
- Git 工作流文档已同步更新时间规则：文档更新时间统一 `YYYY-MM-DD HH:MM:SS` 且必须为真实系统时间。
- 当前迭代策略已切换为"敏捷闭环优先"：分支保护治理项后置，P0 仅保留线上可用性故障闭环与验收留档。
- AGENTS/README/架构/工作流已统一"分支保护后置"口径：当前阶段不作为迭代阻塞条件，统一回补至 `ROADMAP` 持续改进区。
- 分支保护实检已触发一次：当前环境缺少 `GITHUB_TOKEN`，待补管理员 Token 后执行远端 `check/apply` 留档。
- 交易导入规范 v1 已落地：新增 `docs/交易流水YAML导入规范.md`，定义幂等键、冲突策略与最小示例。
- 交易导入 API 已落地：新增 `POST /api/transactions/import_yaml`（`added/skipped/conflicted/warnings` 统计 + 冲突明细）。
- 交易流水查询 API 已落地：新增 `GET /api/transactions`（`all/pending/confirmed` 过滤 + `summary` 汇总 + `data_status`）。
- 交易对账 API 第一阶段已落地：新增 `POST /api/transactions/sync_pending`，按 `fund_nav_daily` 补全 `pending -> confirmed`。
- 交易导入烟测已补齐：新增 `backend/app/api/test_transactions_import_smoke.py`，覆盖幂等跳过、冲突识别、跨用户隔离。
- 交易对账烟测已补齐：`test_sync_pending_transitions_to_confirmed` 覆盖对账补全与状态迁移。
- 交易页前端已接入交易流水面板：支持 `all/pending/confirmed` 过滤、`sync_pending` 一键对账、`data_status` 口径联动与结果反馈。
- 基金详情页已接入交易双态联动：支持按 `fund_id` 拉取交易状态、展示 pending/confirmed 汇总，并可对当前基金执行 `sync_pending`。
- 持仓页基金详情图表区已接入 `data_status` 联动提示：图表文案会按 `confirm_state/asof/note` 实时切换 `已确认/估算中/部分可用`。
- 交易接口增强：`GET /api/transactions` 与 `POST /api/transactions/sync_pending` 已支持 `fund_id` 维度过滤/单基金对账。
- 状态解释验收样例已落地：新增 `docs/状态解释验收样例.md`，沉淀 `data_status` 与 `/api/system/status` 的接口样例、页面验收点与截图规范。
- Gate-D 模板已固化状态解释回归字段：`docs/Gate-D验收证据模板.md` 新增 `data_status`、图表区口径提示、`/api/system/status` 三类必查项。
- 交易页生命周期最小图已落地：新增 `pending -> confirmed -> 计入收益` 三阶段可视化，并按交易汇总自动高亮当前节点。
- 发布流程执行化已闭环：`scripts/check_release_message.py` 现已强制校验发布提交 `文档:` 段必须包含"检查范围 / 更新结论 / 延后项（无则写无）"。
- 交易手工修正闭环后端已落地：新增 `PATCH /api/transactions/{transaction_id}`，支持修正关键字段并写入 `audit_logs`。
- 交易审计能力已落库：新增 `audit_logs` 表与查询能力（按 `entity_type/entity_id` 检索）。
- 交易烟测已扩展：覆盖手工修正接口与审计日志写入（pending -> confirmed + 审计留痕）。
- Mission H 已落地：持仓表新增 `Edit/Audit` 快捷入口，后端新增 `GET /api/holdings/{fund_id}/audit`，支持持仓变更历史回放。
- 风险状态条已前置到持仓页：显示集中度/回撤预估/结构预警摘要，并支持一键平滑跳转到风险中枢详情。
- 图表纪律改造已落地：收益图强制显示 `0%` 基准虚线，新增净值参考图成本线虚线，持仓 Mini-Sparkline 增加"近1月区间标签"与高对比度曲线。
- 前端错误反馈已统一：登录/刷新/设置/交易主链路统一走错误映射层，所有关键错误文案都包含"下一步"操作建议。
- 刷新慢优化第二阶段已落地：`/api/estimate` 在快照命中外新增"基金级增量复用"，仅拉取未命中或过期基金报价，并回传 `incremental_*` 统计。
- 测速探针容错已增强：`network_benchmark` 支持站点级协议配置，并在 TLS EOF 等错误场景自动回退 HTTP，降低误报失败率。
- 周末状态口径已修复：估值引擎在 `cn_hk/us_overseas` 周末休市时将 `confirm_state` 切换到 `confirmed`，避免页面持续显示"估算中"。
- 交易事实口径已接入估值主链路：`/api/estimate` 现联合 `fund_transactions` 汇总，当基金已无 pending 且存在 confirmed 交易时，`confirm_state` 直接进入 `confirmed`，并标记 `yesterday_profit_source=transaction_confirmed`。
- 交易手工修正前端闭环已落地：交易页新增"编辑/审计"操作，可直接调用 `PATCH /api/transactions/{id}` 修正发生时间与状态，并通过 `GET /api/transactions/{id}/audit` 查看审计链路回放。
- 基金别名检索已落地：新增 `fund_alias` 存储与同步，`/api/funds/search`、`/api/funds/suggest` 支持别名命中（如"纳指"），并返回 `aliases/alias_hits` 便于前端展示命中原因。
- `fund_master` 第一阶段已落地：新增主数据真源表并完成 `fund_catalog -> fund_master` 回填迁移，`/api/funds/search`、`/api/funds/suggest`、`/api/funds/{fund_id}` 已透出 `market_group/fund_type/bucket/tags/source` 字段。
- `/api/system/status` 增强已落地：新增 `release.current/online_reference/compare_with_online`，并补充 `transactions_sync_pending` 的 pending/confirmed/synced 统计，前端系统状态页已同步展示。
- 多 Agent 协作模式已优化：跨 Agent 规范、进度与通讯文档统一迁移到共享根目录 `<local>\AGENT`，并移除仓库内副本，避免被各 worktree 分叉。
- 设置中心白屏修复已并入 `dev`：前端补齐抽屉打开、测速模块渲染、异常兜底回归用例，`npm --prefix frontend run build` 与 `npm --prefix frontend run test:run` 验证通过。
- `sync_pending` 入账口径闭环已并入 `dev`：入账后自动失效估值快照缓存，`/api/report/daily` 新增"对账入账"段落，避免交易状态与收益口径短时不一致。
- 基金公共数据采集任务已补齐第一轮工程化能力：`/api/funds/sync` 增加抓取重试、请求间隔频控、失败隔离与任务明细日志（`log_count/recent_logs`）落库。
- 基金同步 API 第二阶段已落地：`/api/funds/sync` 新增 `async_mode` 后台任务化执行，并提供 `running/done/partial/failed` 任务状态与细粒度错误分类日志。
- 基金自动补全第二阶段前端已推进：持仓页新增"新增持仓（自动补全）"入口，支持基金代码联想并自动回填名称/市场标签，提交后直接写入 `/api/holdings` 并回显新增/覆盖结果。
- 基金自动补全第二阶段已收口：持仓表操作列新增"自动补全"，按 `fund_id` 回填 `name/market_group/bucket/tags` 并通过 `PATCH /api/holdings/{fund_id}` 更新，形成"新增+编辑"双入口闭环。

## 2. 本轮（Gate-D）完成内容
- 新增生产部署目录与编排：
`deploy/docker-compose.prod.yml`
`deploy/backend/Dockerfile`
`deploy/frontend/Dockerfile`
`deploy/nginx/nginx.conf`
`deploy/nginx/site.conf`
`deploy/nginx/site.http.conf`
- 新增部署脚本：
`scripts/deploy_prod.sh`
`scripts/update_prod.sh`
`scripts/renew_cert.sh`
`scripts/backup_db.sh`
- 新增部署验收脚本：
`scripts/check_gate_d.py`
- 新增健康检查：
`/api/healthz`

## 3. 当前验收状态
- 本地 Docker 环境下，HTTP 最小闭环 Gate-D 已通过：
`VC_DOMAIN=127.0.0.1 VC_SCHEME=http python scripts/check_gate_d.py`
- 真实域名 HTTPS 验收待执行（需要公网 VPS + 可用域名）。

## 4. 下一步
1. 先处理线上阻塞项：修复 VPS 测速 `Not Found`，并完成一次含测速校验的 Gate-D 实机验收留档。
2. 推进 P1 基金详情 API 第二阶段：完善收益区间统计、完整性标记与异常点标识。
3. 推进 P1.5 消息通道增强：飞书模板化推送重试与发送日志追踪。

## 5. 刷新性能优化（本轮）
- `/api/estimate` 内联返回 `risk_overview`，前端刷新改为单请求链路。
- `/api/risk/overview` 优先复用最新估值快照，避免重复触发外部估值抓取。
- 持仓页刷新时不再二次请求风险接口，降低慢刷新的网络与后端计算开销。

## 6. 本轮补充（2026-02-08 21:04:26）
- 设置中心飞书 webhook 默认改为掩码展示，新增"更新凭据"显式入口，避免默认回显明文。
- 保存策略更新：仅在显式更新时提交新 webhook；输入留空时保持原凭据不变。
- 前端回归新增 2 条用例，覆盖掩码展示、显式更新、留空保持原值。

## 7. 本轮补充（2026-02-08 21:49:46）
- 前端设置中心已对接后端独立凭据写接口：更新飞书 webhook 时改调用 `PUT /api/settings/notifications/feishu/webhook`，不再通过 `PUT /api/settings` 全量回传敏感字段。
- `SettingsDrawer` 保存链路已拆分为"凭据更新"和"普通设置保存"两步：仅当显式输入新 webhook 时才调用凭据接口；普通设置仍走原 settings 保存接口。
- 新增/调整回归用例，覆盖显式更新凭据、留空不更新凭据、普通设置保存不带 `webhook_url`；本地 `npm --prefix frontend run test:run` 与 `npm --prefix frontend run build` 通过。

## 8. 本轮补充（2026-02-10 01:52:47）
- 首页新增 `BenchmarkComparisonPanel`，并在收益曲线面板下方展示组合与多基准对比结果。
- 当基准数据未知时统一显示 `unknown`，并显式提示"基准数据未就绪，当前仅展示组合收益率"。
- 已完成本地验证：
  - `npm --prefix frontend run test:run -- src/components/BenchmarkComparisonPanel.test.jsx`
  - `npm --prefix frontend run test:run -- src/components/PortfolioReturnsPanel.test.jsx`
  - `npm --prefix frontend run build`
